<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.business.friedrich.kuzan.mybank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_bank"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_bank_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Client_PIN_CODE_Activity" />
        <activity android:name=".EmployeePasswordActivity"></activity>
    </application>

</manifest>